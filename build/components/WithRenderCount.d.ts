import React from "react";
import { PropsWithChildren } from "react";
export declare function WithRenderCount({ children, autoRefreshInterval, }: {
    autoRefreshInterval?: number;
} & PropsWithChildren): React.JSX.Element;
