import React, { PropsWithChildren } from "react";
export declare function WithConsole({ children, autoRefreshInterval, }: {
    autoRefreshInterval?: number;
} & PropsWithChildren): React.JSX.Element;
